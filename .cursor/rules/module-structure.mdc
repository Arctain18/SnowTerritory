---
description: SnowTerritory 模块结构规范
globs: **/*Module.java,**/reinforce/**/*.java,**/quest/**/*.java,**/enderstorage/**/*.java,**/stocks/**/*.java
alwaysApply: false
---

# 模块结构规范

每个模块目录结构：

```
{模块名}/
├── {模块名}Module.java    # 入口，enable/disable/reload
├── command/              # 子命令
├── config/               # ConfigManager、DefaultFiles
├── gui/                  # GUI 界面
├── listener/             # 事件监听
├── service/              # 业务逻辑
├── data/                 # 数据模型、DAO 接口
└── storage/              # 存储实现（可选）
```

## 模块入口

- `*Module` 实现 `enable()`、`disable()`、`reload()`
- 通过 `PluginConfig.isModuleEnabled("moduleName")` 控制启用
- 在 `Main.onEnable()` 中按配置初始化模块

## 配置管理

- 模块配置：`DefaultFiles` 用 Java 文本块定义默认 YAML，首次运行时写入 `plugin.getDataFolder()` 子目录
- 配置路径：`plugins/SnowTerritory/{模块}/config.yml`

## 数据访问

- 使用 DAO 接口 + 实现（如 `QuestDatabaseDao`、`LootStorageDao`）
- 存储：SQLite（HikariCP + sqlite-jdbc）
